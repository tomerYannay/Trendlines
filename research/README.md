# Research & one-off scripts

Ad-hoc analysis, tests and experiments. These import the project modules
(`db`, `methods`, `historical_analysis`), so run them from the project root:

    python -m research.test_sequence_analysis

(not `python research/test_sequence_analysis.py`)
